@*Define model*@

@*Define ViewData*@


@*Write Code*@



@model IEnumerable<CineTix.Models.Entity_Classes.Movie>

@{
    ViewData["Title"] = "List of Movies";
    var userRole = Context.Session.GetString("UserRole");
}


<div class="container px-2">
    <h1 class="my-4 text-center" style="color: white">Movies</h1>
    @if (User.Identity.IsAuthenticated)
    {
        <div class="text-end mb-5">
            <a href="@Url.Action("Create", "Movies")" class="btn btn-primary">Add New Movie</a>
        </div>
    }
    @if (Model.Any())
    {
        <div class="d-flex flex-row flex-wrap justify-content-around mb-5">
            @foreach (var movies in Model)
            {
                <div class="card me-3 mb-3" style="width: 18rem; background-color: black;">
                    <a href="@Url.Action("Details", "Movies", new { id = movies.Id })" class="btn">
                        <img src="@movies.ImageUrl" class="card-img-top" alt="@movies.Title" style="height: 27rem; width: 18rem;">
                    </a>
                </div>
            }
        </div>
    }
    else
    {
        <p>No movies available.</p>
    }
</div>